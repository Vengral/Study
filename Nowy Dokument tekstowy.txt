def expanded_form(num):
    my_num = str(num)
    result = ""
    
    for j, i in enumerate(my_num):
        if i != "0":
         result += " + {}{}".format(i,len(my_num[j+1:])*"0")
    
    
        
    return result.strip(" +")